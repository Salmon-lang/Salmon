import {
  print
}

function fib(n) {
  if (n < 2) return n;
  return fib(n - 2) + fib(n - 1);
}
var t := _clock();
var x := [];
for (var i := 0; i < 20; i += 1) {
  x += [];
}
println(x);
println(_clock() - t);
