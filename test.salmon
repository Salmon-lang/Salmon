import {
  print
}

function fib(n) {
  if (n < 2) return n;
  return fib(n - 2) + fib(n - 1);
}
var t := _clock();
var x := [];
println(_length(x));
for(var i := 0; i < 20; i +=1) {
  x += fib(i);
println(_length(x));
}
println_array(x, "\n");
println(_clock() - t);
